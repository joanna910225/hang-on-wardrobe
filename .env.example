# Server-only secret. Never prefix this with EXPO_PUBLIC_ or ship it in the app.
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-5.6-terra
PORT=8787

# iOS Simulator can reach the Mac through loopback. Use the Mac's LAN IP on a phone.
EXPO_PUBLIC_ANALYSIS_API_URL=http://127.0.0.1:8787
